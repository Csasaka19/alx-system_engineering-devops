sudo ufw allow 8080/tcp
sudo ufw enable
sudo ufw status verbose
sudo vim /etc/ufw/before.rules
# Added before the filter section
*nat
:PREROUTING ACCEPT [0:0]
-A PREROUTING -p tcp --dport 8080 -j REDIRECT --to-port 80
COMMIT
#
sudo ufw enable